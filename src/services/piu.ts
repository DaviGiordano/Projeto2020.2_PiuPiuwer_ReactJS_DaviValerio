import axios from 'axios';

/*
export async function getPius(token:string) {
    const response = await axios({
    url: 'http://piupiuwer.polijr.com.br/pius/',
    method: 'GET',
    headers: {
        Authorization: `JWT ${token}`
   }
})
    return response;
}

*/